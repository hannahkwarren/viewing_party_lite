<h1>Viewing Party</h1>

<%= button_to "Register as a User", "/register" %>

<h3>Existing Users:</h3>
<div class="existing-users">
<% @users.each do |user| %>
  <p><%= link_to "#{user.email}'s Dashboard", "/users/#{user.id}" %></p>
<% end %>
</div>
